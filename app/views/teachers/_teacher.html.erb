<div id="<%= dom_id teacher %>">
  <p>
    <strong>Name:</strong>
    <%= teacher.name %>
  </p>
  <% teacher.students.each do |s| %>
    <p>
      <strong>Student:</strong>
      <%= s.name %>
    </p>
  <%end%>

</div>
