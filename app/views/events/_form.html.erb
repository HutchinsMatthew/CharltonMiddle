<%= form_with(model: event) do |form| %>
  <% if event.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% event.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %> 

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>
  <div>
    <%= form.label :startdate, style: "display: block" %>
    <%= form.time_field :startdate %>
  </div> 
  <div>
    <%= form.label :enddate, style: "display: block" %>
    <%= form.time_field :enddate %>
  </div>
  <div>
    <%= form.label :club_id, style: "display: block" %>
    <%= form.select :club_id , options_for_select(@clubs.pluck(:title, :id))%>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
